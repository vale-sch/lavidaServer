{
  "version": 2,
  "builds": [{ "src": "api/**/*.ts", "use": "@vercel/node" }],
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/api/.*",
      "dest": "/api/Serverless-login.ts",
      "methods": ["GET", "POST"],
      "headers": {
        "Access-Control-Allow-Origin": "http://127.0.0.1:5500/" 
      }
    }
  ]
}
